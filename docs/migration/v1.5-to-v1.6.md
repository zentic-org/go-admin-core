# go-admin-core v1.5 → v1.6 迁移指南

## 概述

go-admin-core v1.6.0 进行了重要的架构优化,主要目标是:

1. **简化包结构** - 将常用包从 `sdk/pkg/*` 提升到根目录
2. **优化命名** - 缩短过长的包名 (`observability` → `observe`)
3. **解决冲突** - 消除包名冲突 (`tools/gorm/logger` → `tools/gorm/gormlog`)
4. **向后兼容** - 提供完整的兼容层,新旧路径在 v1.6.x 期间都可用

**重要**: 本次升级是**非破坏性的**,旧代码无需立即修改即可继续工作。

## 变更概览

### 包路径变更

| 旧路径 | 新路径 | 说明 |
|--------|--------|------|
| `sdk/pkg/captcha` | `captcha` | 验证码功能 |
| `sdk/pkg/jwtauth` | `jwtauth` | JWT认证 |
| `sdk/pkg/response` | `response` | HTTP响应封装 |
| `sdk/pkg/casbin` | `casbin` | 权限管理 |
| `observability/audit` | `observe/audit` | 审计日志 |
| `tools/gorm/logger` | `tools/gorm/gormlog` | GORM日志适配器 |

### 兼容性时间线

```
v1.6.0-beta (2025-10-24)
  ├─ 新旧路径都可用
  └─ 旧路径标记为 Deprecated
  
v1.6.0 (2025-11-07)
  ├─ 稳定版发布
  └─ 旧路径显示废弃警告
  
v1.9.x (2025-11-08 ~ 2026-04-30)
  ├─ 维护期
  └─ 建议迁移到新路径
  
v2.0.0 (2026-05-01)
  └─ 旧路径移除 ⚠️
```

## 迁移方法

### 方法 1: 自动迁移 (推荐)

使用官方提供的自动迁移脚本:

```bash
# 1. 下载迁移脚本
curl -O https://raw.githubusercontent.com/go-admin-team/go-admin-core/dev/tools/migrate-v1.6.sh

# 2. 添加执行权限
chmod +x migrate-v1.6.sh

# 3. 运行迁移 (会自动备份)
./migrate-v1.6.sh
```

**脚本功能**:
- ✅ 自动检测需要更新的文件
- ✅ 创建带时间戳的完整备份
- ✅ 批量替换所有导入路径
- ✅ 运行 `go mod tidy` 和编译测试
- ✅ 生成详细的迁移报告

**示例输出**:
```
================================================
  go-admin-core v1.6.0 自动迁移工具
================================================

[INFO] 检测到 Go 项目: github.com/go-admin-team/go-admin-pro
[SUCCESS] 检测到 go-admin-core 依赖
[INFO] 创建备份: backup_before_v1.8_20251017_150000
[INFO] 找到 15 个文件需要更新
[SUCCESS] 已更新 15 个文件
[SUCCESS] 项目编译成功
[SUCCESS] 迁移完成！
```

### 方法 2: 手动迁移

#### 步骤 1: 更新依赖版本

```bash
# 更新到 v1.6.0
go get github.com/go-admin-team/go-admin-core@v1.6.0

# 整理依赖
go mod tidy
```

#### 步骤 2: 批量替换导入路径

使用 IDE 的"查找并替换"功能(支持正则表达式):

**1. 更新 captcha**
```
查找: github\.com/go-admin-team/go-admin-core/sdk/pkg/captcha
替换: github.com/go-admin-team/go-admin-core/captcha
```

**2. 更新 jwtauth**
```
查找: github\.com/go-admin-team/go-admin-core/sdk/pkg/jwtauth
替换: github.com/go-admin-team/go-admin-core/jwtauth
```

**3. 更新 response**
```
查找: github\.com/go-admin-team/go-admin-core/sdk/pkg/response
替换: github.com/go-admin-team/go-admin-core/response
```

**4. 更新 casbin**
```
查找: github\.com/go-admin-team/go-admin-core/sdk/pkg/casbin
替换: github.com/go-admin-team/go-admin-core/casbin
```

**5. 更新 observe (如有使用)**
```
查找: github\.com/go-admin-team/go-admin-core/observability/audit
替换: github.com/go-admin-team/go-admin-core/observe/audit
```

**6. 更新 gormlog (如有使用)**
```
查找: github\.com/go-admin-team/go-admin-core/tools/gorm/logger
替换: github.com/go-admin-team/go-admin-core/tools/gorm/gormlog
```

#### 步骤 3: 验证更改

```bash
# 格式化代码
go fmt ./...

# 整理依赖
go mod tidy

# 编译测试
go build ./...

# 运行测试
go test ./...
```

## 详细迁移示例

### 示例 1: 更新 Captcha 使用

**迁移前**:
```go
package admin

import (
    "github.com/gin-gonic/gin"
    "github.com/go-admin-team/go-admin-core/sdk/pkg/captcha"
)

func GetCaptcha(c *gin.Context) {
    id, b64s, _, err := captcha.DriverDigitFunc()
    // ...
}
```

**迁移后**:
```go
package admin

import (
    "github.com/gin-gonic/gin"
    "github.com/go-admin-team/go-admin-core/captcha"  // ✅ 新路径
)

func GetCaptcha(c *gin.Context) {
    id, b64s, _, err := captcha.DriverDigitFunc()
    // 使用方式完全相同
}
```

### 示例 2: 更新 JWT 认证

**迁移前**:
```go
package middleware

import (
    jwt "github.com/go-admin-team/go-admin-core/sdk/pkg/jwtauth"
)

func AuthInit() (*jwt.GinJWTMiddleware, error) {
    return jwt.New(&jwt.GinJWTMiddleware{
        // ...
    })
}
```

**迁移后**:
```go
package middleware

import (
    jwt "github.com/go-admin-team/go-admin-core/jwtauth"  // ✅ 新路径
)

func AuthInit() (*jwt.GinJWTMiddleware, error) {
    return jwt.New(&jwt.GinJWTMiddleware{
        // 使用方式完全相同
    })
}
```

### 示例 3: 更新响应处理

**迁移前**:
```go
package apis

import (
    "github.com/gin-gonic/gin"
    "github.com/go-admin-team/go-admin-core/sdk/pkg/response"
)

func GetUser(c *gin.Context) {
    response.OK(c, user, "查询成功")
}
```

**迁移后**:
```go
package apis

import (
    "github.com/gin-gonic/gin"
    "github.com/go-admin-team/go-admin-core/response"  // ✅ 新路径
)

func GetUser(c *gin.Context) {
    response.OK(c, user, "查询成功")
    // 使用方式完全相同
}
```

### 示例 4: 更新 Casbin

**迁移前**:
```go
package database

import (
    mycasbin "github.com/go-admin-team/go-admin-core/sdk/pkg/casbin"
    "gorm.io/gorm"
)

func InitCasbin(db *gorm.DB) {
    enforcer := mycasbin.Setup(db, "casbin_rule")
}
```

**迁移后**:
```go
package database

import (
    mycasbin "github.com/go-admin-team/go-admin-core/casbin"  // ✅ 新路径
    "gorm.io/gorm"
)

func InitCasbin(db *gorm.DB) {
    enforcer := mycasbin.Setup(db, "casbin_rule")
    // 使用方式完全相同
}
```

### 示例 5: 更新 GORM Logger

**迁移前**:
```go
package database

import (
    . "github.com/go-admin-team/go-admin-core/tools/gorm/logger"
    "gorm.io/gorm/logger"
)

func initDB() {
    gormLogger := New(logger.Config{
        SlowThreshold: 200 * time.Millisecond,
    })
}
```

**迁移后**:
```go
package database

import (
    . "github.com/go-admin-team/go-admin-core/tools/gorm/gormlog"  // ✅ 新路径
    "gorm.io/gorm/logger"
)

func initDB() {
    gormLogger := New(logger.Config{
        SlowThreshold: 200 * time.Millisecond,
    })
    // 使用方式完全相同
}
```

## API 兼容性

### ✅ 完全兼容的 API

所有公共 API 保持 100% 兼容,包括:

**captcha**:
- `SetStore(s base64Captcha.Store)`
- `DriverStringFunc() (id, b64s, answer string, err error)`
- `DriverDigitFunc() (id, b64s, answer string, err error)`
- `Verify(id, code string, clear bool) bool`

**jwtauth**:
- `type MapClaims`
- `type GinJWTMiddleware`
- `New(m *GinJWTMiddleware) (*GinJWTMiddleware, error)`
- `ExtractClaims(c *gin.Context) MapClaims`
- `ExtractClaimsFromToken(token *jwt.Token) MapClaims`
- `GetToken(c *gin.Context) string`

**response**:
- `Error(c *gin.Context, code int, err error, msg string)`
- `OK(c *gin.Context, data interface{}, msg string)`
- `PageOK(c *gin.Context, result interface{}, count int, pageIndex int, pageSize int, msg string)`
- `Custum(c *gin.Context, data gin.H)`

**casbin**:
- `type Logger`
- `Setup(db *gorm.DB, tableName string) *casbin.SyncedEnforcer`

**observe/audit**:
- `type Log`
- `type Record`
- `type Stream`
- 所有原有函数和常量

**tools/gorm/gormlog**:
- `New(config logger.Config) logger.Interface`

### ⚠️ 不兼容的情况

**无** - 本次升级完全向后兼容!

## 常见问题

### Q1: 必须立即迁移吗?

**A**: 不需要。旧路径在 v1.8.x 和 v1.9.x 期间仍然可用,有6个月的过渡期。但建议尽早迁移以避免未来的问题。

### Q2: 迁移会破坏现有代码吗?

**A**: 不会。我们提供了完整的兼容层,旧代码无需任何修改即可继续工作。

### Q3: 如何知道哪些文件需要更新?

**A**: 使用自动迁移脚本,它会自动扫描并显示所有需要更新的文件。或者手动搜索:

```bash
grep -r "sdk/pkg/captcha\|sdk/pkg/jwtauth\|sdk/pkg/response\|sdk/pkg/casbin" --include="*.go" .
```

### Q4: 迁移后编译失败怎么办?

**A**: 
1. 检查是否正确更新了所有导入路径
2. 运行 `go mod tidy` 清理依赖
3. 检查 `go.mod` 中的版本是否正确
4. 查看错误信息,确认是否有拼写错误

### Q5: 可以只迁移部分包吗?

**A**: 可以。您可以逐个包迁移,或者混合使用新旧路径。但建议一次性全部迁移,保持代码一致性。

### Q6: 如何回滚?

**A**: 
- 如果使用自动脚本: 恢复备份目录中的文件
- 如果手动迁移: 使用 `git revert` 或 `git reset`
- 降级依赖: `go get github.com/go-admin-team/go-admin-core@v1.5.x`

### Q7: IDE 显示 Deprecated 警告正常吗?

**A**: 如果您还在使用旧路径,显示 Deprecated 警告是正常的。这是提醒您迁移到新路径。迁移后警告会消失。

### Q8: go-admin-pro 需要特殊处理吗?

**A**: 不需要。使用相同的迁移方法即可。建议先在 go-admin-core 上测试,然后再迁移 go-admin-pro。

## 验证清单

迁移完成后,请确认以下事项:

- [ ] 所有 `sdk/pkg/captcha` 已替换为 `captcha`
- [ ] 所有 `sdk/pkg/jwtauth` 已替换为 `jwtauth`
- [ ] 所有 `sdk/pkg/response` 已替换为 `response`
- [ ] 所有 `sdk/pkg/casbin` 已替换为 `casbin`
- [ ] 所有 `observability/audit` 已替换为 `observe/audit` (如有)
- [ ] 所有 `tools/gorm/logger` 已替换为 `tools/gorm/gormlog` (如有)
- [ ] `go mod tidy` 执行成功
- [ ] `go build ./...` 编译通过
- [ ] `go test ./...` 测试通过
- [ ] 应用程序正常运行
- [ ] 没有 import cycle 错误
- [ ] IDE 不再显示 Deprecated 警告

## 获取帮助

如果遇到问题:

1. **查看完整文档**: [docs/migration/v1.6.0-plan.md](./v1.6.0-plan.md)
2. **搜索已知问题**: [GitHub Issues](https://github.com/go-admin-team/go-admin-core/issues)
3. **提交新问题**: [创建 Issue](https://github.com/go-admin-team/go-admin-core/issues/new)
4. **社区讨论**: [GitHub Discussions](https://github.com/go-admin-team/go-admin-core/discussions)

## 参考资源

- [v1.6.0 重构计划](./v1.6.0-plan.md) - 详细的技术设计文档
- [自动迁移工具](../../tools/README.md) - 工具使用说明
- [CHANGELOG](../../CHANGELOG.md) - 完整变更日志
- [GitHub Release](https://github.com/go-admin-team/go-admin-core/releases) - 发布说明

## 反馈

我们非常重视您的反馈!如果您:
- ✅ 成功完成迁移 - 请在 [Discussions](https://github.com/go-admin-team/go-admin-core/discussions) 分享经验
- ⚠️ 遇到问题 - 请提交 [Issue](https://github.com/go-admin-team/go-admin-core/issues)
- 💡 有改进建议 - 欢迎提交 [Pull Request](https://github.com/go-admin-team/go-admin-core/pulls)

---

**最后更新**: 2025年10月17日  
**适用版本**: go-admin-core v1.6.0+
