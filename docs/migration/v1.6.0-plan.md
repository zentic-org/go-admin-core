# go-admin-core v1.6.0 重构计划

**版本**: v1.6.0-beta  
**日期**: 2025-10-17  
**状态**: 🚧 执行中  
**重构类型**: 渐进式重构（提供兼容层）

---

## 📋 执行摘要

本次重构旨在优化 go-admin-core 的包结构，移除 `sdk/pkg` 反模式，同时保持向后兼容性。

### 核心目标

1. ✅ **消除 sdk/pkg 反模式** - 将常用包提升到根目录
2. ✅ **简化包名** - 缩短过长的包名
3. ✅ **解决命名冲突** - 避免 import 歧义
4. ✅ **提供兼容层** - 6 个月过渡期
5. ✅ **自动化迁移工具** - 降低用户迁移成本

### 破坏性变更

⚠️ **本次重构包含破坏性变更，但提供 6 个月兼容期**

- Import 路径变更（旧路径仍可用，但标记为 deprecated）
- 计划在 v2.0.0 完全移除兼容层

---

## 🗺️ 包重组计划

### 第一阶段：提升常用包到根目录

#### 1. captcha (验证码)

**当前位置**: `sdk/pkg/captcha`  
**新位置**: `captcha`  
**原因**: 高频使用的公共包，应在根目录

```go
// 旧导入 (v1.7.0 及之前)
import "github.com/go-admin-team/go-admin-core/sdk/pkg/captcha"

// 新导入 (v1.6.0+)
import "github.com/go-admin-team/go-admin-core/captcha"
```

**影响范围**:
- go-admin-pro: `app/admin/apis/sys_login.go`
- 预计外部使用者: 中等

#### 2. jwtauth (JWT 认证)

**当前位置**: `sdk/pkg/jwtauth`  
**新位置**: `jwtauth`  
**原因**: 核心认证功能，应在根目录

```go
// 旧导入
import "github.com/go-admin-team/go-admin-core/sdk/pkg/jwtauth"

// 新导入
import "github.com/go-admin-team/go-admin-core/jwtauth"
```

**影响范围**:
- go-admin-pro: `common/middleware/auth.go`, `common/middleware/handler.go`
- 预计外部使用者: 高

#### 3. response (响应封装)

**当前位置**: `sdk/pkg/response`  
**新位置**: `response`  
**原因**: API 响应工具，高频使用

```go
// 旧导入
import "github.com/go-admin-team/go-admin-core/sdk/pkg/response"

// 新导入
import "github.com/go-admin-team/go-admin-core/response"
```

**影响范围**:
- go-admin-pro: 所有 APIs 文件
- 预计外部使用者: 非常高

#### 4. casbin (权限管理)

**当前位置**: `sdk/pkg/casbin`  
**新位置**: `casbin`  
**原因**: 权限核心，应在根目录

```go
// 旧导入
import "github.com/go-admin-team/go-admin-core/sdk/pkg/casbin"

// 新导入
import "github.com/go-admin-team/go-admin-core/casbin"
```

**影响范围**:
- go-admin-pro: `common/middleware/permission.go`
- 预计外部使用者: 中等

---

### 第二阶段：简化包名

#### 5. observability → observe

**当前位置**: `observability/audit`  
**新位置**: `observe/audit`  
**原因**: 14 个字符太长，不便输入

```go
// 旧导入
import "github.com/go-admin-team/go-admin-core/observability/audit"

// 新导入
import "github.com/go-admin-team/go-admin-core/observe/audit"
```

**影响范围**: 内部使用，外部影响小

---

### 第三阶段：解决命名冲突

#### 6. sdk/config 冲突

**问题**: 与根目录 `config/` 冲突

**解决方案**: 
- 选项 A: 重命名为 `sdkconfig`
- 选项 B: 移入 `internal/sdk/config` (v2.0.0)

**当前决策**: 暂时保留，v2.0.0 移入 internal

#### 7. tools/gorm/logger 冲突

**问题**: 与根目录 `logger/` 冲突

**解决方案**: 重命名为 `gormlog`

```go
// 旧导入
import "github.com/go-admin-team/go-admin-core/tools/gorm/logger"

// 新导入
import "github.com/go-admin-team/go-admin-core/tools/gorm/gormlog"
```

---

## 🔄 新旧路径对照表

| 功能 | v1.7.0 路径 | v1.6.0 路径 | 优先级 |
|------|------------|------------|--------|
| 验证码 | `sdk/pkg/captcha` | `captcha` | P0 |
| JWT认证 | `sdk/pkg/jwtauth` | `jwtauth` | P0 |
| 响应封装 | `sdk/pkg/response` | `response` | P0 |
| 权限管理 | `sdk/pkg/casbin` | `casbin` | P0 |
| 可观测性 | `observability/audit` | `observe/audit` | P1 |
| GORM日志 | `tools/gorm/logger` | `tools/gorm/gormlog` | P1 |

---

## 🛡️ 兼容层设计

### 兼容层实现

在旧路径保留 `deprecated.go` 文件，重新导出新位置的 API：

```go
// sdk/pkg/captcha/deprecated.go
package captcha

import (
    newcaptcha "github.com/go-admin-team/go-admin-core/captcha"
)

// Deprecated: 使用 github.com/go-admin-team/go-admin-core/captcha 替代
// 本包将在 v2.0.0 移除
type Config = newcaptcha.Config

// Deprecated: 使用 github.com/go-admin-team/go-admin-core/captcha 替代
var New = newcaptcha.New

// Deprecated: 使用 github.com/go-admin-team/go-admin-core/captcha 替代
var Generate = newcaptcha.Generate

// ... 导出所有公共 API
```

### 兼容层特性

✅ **功能完整**: 所有 API 100% 可用  
✅ **编译告警**: 使用 `// Deprecated:` 标记  
✅ **IDE 提示**: 现代 IDE 会提示废弃  
✅ **过渡期**: 6 个月（至 v2.0.0）

---

## 🔧 迁移工具

### 自动迁移脚本

提供 `tools/migrate-v1.6.sh` 脚本：

```bash
#!/bin/bash
# 自动迁移 import 路径

# 使用方法
./tools/migrate-v1.6.sh /path/to/your/project
```

**功能**:
1. ✅ 自动替换 import 路径
2. ✅ 生成迁移报告
3. ✅ 支持预览模式（--dry-run）
4. ✅ 备份原文件

### 手动迁移

**步骤 1**: 查找旧导入

```bash
grep -r "sdk/pkg/captcha" .
grep -r "sdk/pkg/jwtauth" .
grep -r "sdk/pkg/response" .
grep -r "sdk/pkg/casbin" .
```

**步骤 2**: 替换导入路径

使用编辑器的全局替换功能：

```
查找: github.com/go-admin-team/go-admin-core/sdk/pkg/captcha
替换: github.com/go-admin-team/go-admin-core/captcha
```

**步骤 3**: 运行测试

```bash
go mod tidy
go test ./...
```

---

## 📅 迁移时间表

### v1.6.0-beta (2025-10-24)

- ✅ 新包位置创建
- ✅ 兼容层实现
- ✅ 迁移工具发布
- ✅ Beta 测试开始

### v1.6.0 正式版 (2025-11-07)

- ✅ 根据 Beta 反馈调整
- ✅ 完善文档
- ✅ 正式发布

### v1.9.x (2025-11-08 - 2026-04-30)

- ⏳ 兼容层维护期
- ⏳ 鼓励用户迁移
- ⏳ 收集反馈

### v2.0.0 (2026-05-01)

- 🔴 完全移除兼容层
- 🔴 强制使用新路径

---

## 🎯 成功标准

### 技术标准

- [ ] 所有新包位置代码正常工作
- [ ] 兼容层 100% 覆盖旧 API
- [ ] go-admin-core 内部全部使用新路径
- [ ] go-admin-pro 集成测试通过
- [ ] 迁移工具可用且准确

### 用户标准

- [ ] 迁移指南清晰易懂
- [ ] 用户反馈 >= 80% 满意
- [ ] 迁移成功率 >= 95%
- [ ] GitHub Issues < 5 个重大问题

---

## 🚨 风险评估

### 高风险项

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| go-admin-pro 不兼容 | 🔴 高 | 同步更新，充分测试 |
| 用户抵制变更 | 🟡 中 | 6 个月过渡期，自动工具 |
| 兼容层不完整 | 🔴 高 | 100% API 覆盖测试 |
| 迁移工具有 bug | 🟡 中 | 提供 --dry-run 预览 |

### 应对计划

**R1: go-admin-pro 不兼容**
- 提供完整的迁移 PR
- 同步发布时间
- 充分集成测试

**R2: 用户抵制**
- 提前 2 周通知
- 提供自动迁移工具
- 延长过渡期到 6 个月

**R3: 兼容层问题**
- 完整的单元测试
- Beta 用户测试
- 快速修复机制

---

## 📞 沟通计划

### 提前通知 (发布前 2 周)

- [ ] GitHub Discussion 发布公告
- [ ] README 添加通知横幅
- [ ] 发送邮件通知（如有订阅列表）

### Beta 测试征集

- [ ] 邀请活跃用户参与测试
- [ ] 建立反馈渠道（Issue 模板）
- [ ] 每日监控反馈

### 正式发布

- [ ] GitHub Release 发布
- [ ] 文档站点更新
- [ ] 社交媒体宣传

---

## 📝 待办事项

### 当前阶段（准备）

- [x] 编写重构计划文档 ✅
- [ ] 创建分支策略
- [ ] 发送用户通知

### 下一阶段（实施）

- [ ] 创建新包位置
- [ ] 实现兼容层
- [ ] 更新内部引用
- [ ] 编写迁移工具

### 最终阶段（发布）

- [ ] 编写迁移指南
- [ ] Beta 测试
- [ ] 正式发布

---

## 🔗 相关文档

- [架构审查报告](../../ARCHITECTURE_REVIEW_REPORT.md)
- [v1.7 to v1.8 迁移指南](./v1.5-to-v1.6.md) (待创建)
- [v2.0 计划](./v2.0-package-visibility.md) (待创建)

---

**计划状态**: 🚧 执行中  
**下次更新**: 实施完成后  
**负责人**: 开发团队
